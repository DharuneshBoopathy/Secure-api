# Core API
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.14.2      # GHSA-4xgf-cpjx-pc3j
python-multipart>=0.0.31

# Security floors for transitive dependencies.
#
# These are pulled in by fastapi[standard] / httpx rather than imported
# directly, so nothing above would raise them on its own — but pip-audit
# flags the older releases, and an unpinned `>=` floor is only as good as the
# lowest version it still permits. Raise these together with the advisory
# that motivated them so the reason survives the next upgrade.
starlette>=1.3.1               # PYSEC-2026-161/248/249/2280/2281
urllib3>=2.7.0                 # PYSEC-2026-141/142
idna>=3.15                     # PYSEC-2026-215
click>=8.3.3                   # PYSEC-2026-2132

# Database
sqlalchemy>=2.0.36
pymysql>=1.1.1
# Fernet encryption for stored provider credentials (app/services/crypto.py)
# rides on this, so its floor is a security floor.
cryptography>=46.0.7           # PYSEC-2026-36, GHSA-537c-gmf6-5ccf
alembic>=1.13.0

# Scheduling & HTTP
apscheduler>=3.10.4
httpx>=0.27.0
PyJWT>=2.13.0
bcrypt>=4.2.0
slowapi>=0.1.9
pyotp>=2.9.0

# Optional Redis integration: ingest queue (app/services/queue_service.py,
# app/worker.py) and distributed rate-limit storage (app/routers/auth.py).
# Only used when REDIS_URL is set — see app/config.py.
redis>=5.0.0

# Distributed tracing (OpenTelemetry). No-op / console-only unless
# OTEL_EXPORTER_OTLP_ENDPOINT is set — see app/tracing.py.
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-sqlalchemy>=0.48b0
opentelemetry-exporter-otlp-proto-http>=1.27.0

# ML / analysis
scikit-learn>=1.5.0
numpy>=1.26.0
pandas>=2.2.0

# PCAP parsing (Wireshark exports / tshark) — pure Python
dpkt>=1.9.8

# Metrics for Grafana (via Prometheus scrape)
prometheus-client>=0.21.0

# OpenAPI handling
PyYAML>=6.0.2
types-PyYAML>=6.0.12
